import SwiftUI

enum OverlayTheme {
    static let panelCornerRadius: CGFloat = 14
    static let controlCornerRadius: CGFloat = 10
    static let compactCornerRadius: CGFloat = 8
    static let smallButtonCornerRadius: CGFloat = 7
    static let iconSize: CGFloat = 52
    static let hairline = Color.white.opacity(0.18)
    static let separator = Color.white.opacity(0.14)
    static let quietFill = Color.white.opacity(0.10)
    static let strongerFill = Color.white.opacity(0.16)
    static let assistantBubble = Color.white.opacity(0.12)
    static let userBubble = Color.white.opacity(0.18)
    static let answerSurface = Color.white.opacity(0.16)
    static let askSurface = Color.white.opacity(0.12)
    static let invertedAccent = Color.white.opacity(0.92)
    static let invertedForeground = Color(red: 0.090, green: 0.073, blue: 0.086)
    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.78)
    static let tertiaryText = Color.white.opacity(0.68)
    static let quaternaryText = Color.white.opacity(0.58)
    static let doneText = Color.white.opacity(0.56)
    static let highlightStroke = Color.white.opacity(0.88)
    static let highlightGlow = Color.white.opacity(0.22)
    static let focusDim = Color.black.opacity(0.48)
}
